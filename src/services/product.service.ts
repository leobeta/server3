import Product from '../models/product';

// export const getAllProducts = async (filters: Object, options: Object) => {
//   const products = await Product.find(filters, options);
//   return products;
// }

export class productService {
  public getAllProducts() {
    console.log('hey')
  }
}